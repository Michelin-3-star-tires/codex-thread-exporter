# 修改记录

## 2026-04-06

- 创建线程查询导出器项目骨架
- 规划交互式查询流程
- 预留线程检索、Markdown 导出和测试验证模块

## 2026-04-06 22:48

- 完成线程检索、对话导出和交互式 CLI 的实现
- 增加 pytest 测试，覆盖检索和导出两条主链路
- 完成 `uvx ruff check .`、`uvx ruff format .` 和 `uv run pytest` 验证
- 删除根目录下这次排查用的临时脚本

## 2026-04-06 22:52

- 新增顶层 `run.py` 作为统一启动入口
- 将导出目录改为按日期和 thread id 分层保存
- 更新 README 中的运行方式与导出路径说明

## 2026-04-06 23:10

- 将元信息级别默认固定为“全部信息（推荐）”
- 将导出目录默认固定为 `output`
- 仅在用户明确选择自定义时才再次询问导出目录

## 2026-04-06 23:11

- 将“是否附带原始消息 JSON”改为静默默认 `n`
- 去掉“是否需要自定义导出目录”的额外交互
- 导出目录继续固定使用 `output`

## 2026-04-06 23:14

- 将导出目录扁平化为 `output/YYYYMMDD/`
- 将单个 thread 文件名改为 `HHMMSS_thread_id.md`
- 导出路径改为以线程创建时间为基准，避免后续更新导致文件位置漂移

## 2026-04-06 23:25

- 导出 Markdown 仅保留对话正文，去掉线程元信息块
- 忽略 `developer` 消息
- 连续 `assistant` 消息只保留最后一条

## 2026-04-06 23:28

- 将导出模式固定为阅读版，并在代码内预留完整版开关
- 将文件名改为“创建时间 + 标题/首句 slug + thread id”
- 在 Markdown 消息标题中区分 `user` 和 `assistant`，并加上符号标识

## 2026-04-06 23:34

- 将 Markdown 一级标题改为本次查询输入内容
- 去掉消息前的中间分区标题，正文直接进入对话消息
- 将用户标题样式改为 `👤 User提问`，助手标题样式改为 `🤖 AI回答`

## 2026-04-06 23:40

- 将消息颜色标识切回蓝色和橙色
- 保持角色文案为 `User提问` 与 `AI回答`

## 2026-04-07

- 将项目对外名称统一为 `Codex 对话查询导出工具`
- 更新启动横幅与 README 标题，便于目录名和程序展示名保持一致

## 2026-04-08 17:55

- 新建 `git worktree` 开源分支，保留本地版本不受影响
- 将数据库默认定位改为 `~/.codex/state_5.sqlite`，并支持 `--db-path` 和 `CODEX_STATE_DB`
- 清理 README 中的本机绝对路径说明，补充公开仓库安装与运行方式
- 为项目补充 `MIT` 许可证、命令行入口和基础 CI 工作流
- 调整测试数据中的工作区路径，避免出现个人本地目录痕迹

## 2026-04-08 18:02

- 移除 `.githooks/commit-msg` 中对中文提交主题的强制约束
- 将 README 重新整理为更完整的开源项目首页，并补充英文摘要
- 新增 `CONTRIBUTING.md`，明确贡献流程、测试命令和提交规范

## 2026-04-08 18:05

- 新增 `.github/ISSUE_TEMPLATE/`，提供问题反馈和功能建议模板
- 新增 `.github/pull_request_template.md`，统一 Pull Request 提交结构

## 2026-04-08 18:07

- 新增 `CODE_OF_CONDUCT.md`，明确公开协作中的行为边界
- 在 README 中补充社区准则入口链接
- 在 `CONTRIBUTING.md` 中补充行为准则阅读提示
